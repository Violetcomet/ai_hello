# IDE and Editor files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.code-workspace

# Operating System Files
.DS_Store
Thumbs.db

# Build artifacts (if you ever add a build process)
dist/
build/
.cache/

# Dependency directories (if you ever use npm, yarn, etc.)
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (if you ever add a backend or local config)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# API Keys (even though ours is blank, good practice if you ever add a real one)
api_keys.js
*.key